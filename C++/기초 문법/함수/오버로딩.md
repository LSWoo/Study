## 오버로딩
- 중복 정의 : 함수 이름의 재사용
```C++
int Add(int a, int b)
{
  int result = a + b;
  count << reuslt << endl;
  return result;
}
float Add(float a, float b)
{
  float result = a + b;
  count << reuslt << endl;
  return result;
}

main()
{
  Add(1, 2);
  Add(1.5f, 2.4f);
}
```
